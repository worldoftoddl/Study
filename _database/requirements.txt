# === K-IFRS RAG Pipeline Requirements ===
# Python 3.12+
#
# 설치 방법:
#   1) python3 -m venv .venv && source .venv/bin/activate
#   2) CPU-only torch (선택): pip install torch --index-url https://download.pytorch.org/whl/cpu
#   3) pip install -r requirements.txt
#   4) pip install --no-deps langchain-upstage==0.7.7  # tokenizers 버전 충돌 회피

# --- LLM & Embedding ---
anthropic==0.84.0
langchain-anthropic==1.3.4
langchain-openai==1.1.10
openai==2.24.0
tiktoken==0.12.0

# --- LangChain Core ---
langchain-core==1.2.17
langchain-classic==1.0.1
langchain-community==0.4.1
langchain-text-splitters==1.1.1

# --- LangGraph (Agentic RAG) ---
langgraph==1.0.10
langgraph-checkpoint==4.0.1
langgraph-prebuilt==1.0.8
langgraph-sdk==0.3.9
langsmith==0.7.12

# --- Vector DB & Search ---
psycopg[binary]==3.2.6
psycopg-pool==3.2.6
pgvector==0.3.6
rank-bm25==0.2.2
networkx>=3.4

# --- Reranker ---
FlagEmbedding==1.3.5
sentence-transformers==5.2.3
cohere==5.20.7

# --- Korean NLP ---
kiwipiepy==0.22.2

# --- PDF Processing ---
pymupdf4llm==0.3.4
pypdf==6.7.5

# --- Data & Utilities ---
python-dotenv==1.2.2
tqdm==4.67.3
pandas==3.0.1
numpy==2.4.2
scikit-learn==1.8.0

# --- Deep Learning (Reranker / Embedding models) ---
torch==2.10.0
transformers==4.57.6
tokenizers>=0.22.0,<=0.23.0
safetensors==0.7.0
accelerate==1.13.0
peft==0.18.1

# --- Jupyter ---
ipykernel==7.2.0
ipython==9.10.0

# --- langchain-upstage (별도 설치) ---
# langchain-upstage==0.7.7 은 tokenizers<0.21.0 을 요구하지만,
# transformers==4.57.6 이 tokenizers>=0.22.0 을 요구하므로 충돌 발생.
# 실제로는 tokenizers>=0.22 로 정상 동작하므로 --no-deps 로 설치:
#   pip install --no-deps langchain-upstage==0.7.7
